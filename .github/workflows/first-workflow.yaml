name: First workflow

on:
  push:
    branches:
      - "main"
      - "develop"
      - "release"
jobs:
  lab-job:
    runs-on: ubuntu-latest
    steps:
      - name: Start job
        run: echo 'Hello DevOPs'
